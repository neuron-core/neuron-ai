---
name: with-fallback
description: A skill that includes fallback behavior
---

# With Fallback

A skill demonstrating fallback behavior.

## Trigger
When the user needs something done with a safety net.

## Fallback
If the primary approach fails, try the alternative method. If that also fails, inform the user and suggest manual intervention.
